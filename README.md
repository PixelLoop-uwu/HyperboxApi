# Hyperbox API

Hyperbox API — это REST API для управления модпаками, ресурсами, скинами и учётными записями пользователей в игровой экосистеме Hyperbox.

![buhFlip-4x](https://github.com/user-attachments/assets/d286f39e-3d85-4a8c-b98f-c26d498f2e61)

## 📋 О проекте

Hyperbox API предоставляет интерфейс для:
- 👤 **Управления пользователями** — регистрация, аутентификация, управление учётными записями
- 📦 **Управления модпаками** — загрузка, хранение и распределение игровых модпаков
- 🎨 **Управления ресурсами** — хранение текстур, шейдеров и других игровых ресурсов
- 👕 **Управления скинами** — загрузка и управление скинами игроков

## 🚀 Стек технологий

- **Python 3.11+**
- **FastAPI** — веб-фреймворк
- **SQLAlchemy 2.0+** — ORM
- **aiosqlite** — асинхронная работа с БД
- **JWT (python-jose)** — аутентификация
- **Loguru** — логирование
- **Docker** — контейнеризация
- **aiorcon** / **rcon** — взаимодействие с игровыми серверами

## 📁 Структура проекта

```
HBApi/
├── auth/                 # Модуль аутентификации
│   ├── jwt.py           # JWT токены
│   └── dependencies.py   # Зависимости FastAPI
├── core/                # Основной функционал
│   ├── database/        # Слой БД
│   │   ├── models.py    # SQLAlchemy модели
│   │   ├── session.py   # Сессия БД
│   │   └── users.py     # Операции с пользователями
│   ├── routes/          # API маршруты для пользователей
│   ├── services/        # Бизнес-логика
│   └── schemas.py       # Pydantic схемы
├── launcher/            # Модуль управления модпаками и ресурсами
│   ├── routes/          # API маршруты (модпаки, ресурсы, скины)
│   ├── services/        # Бизнес-логика (обработка скинов)
│   └── DATA/            # Данные приложения
├── main.py              # Точка входа приложения
├── config.py            # Конфигурация
├── pyproject.toml       # Зависимости проекта
└── docker-compose.yaml  # Docker конфигурация
```

## 📚 API Маршруты

| Модуль | Префикс | Описание |
|--------|---------|---------|
| Users | `/users` | Управление учётными записями пользователей |
| Modpacks | `/modpacks` | Управление модпаками |
| Resources | `/resources` | Управление игровыми ресурсами |
| Skins | `/skins` | Управление скинами игроков |

## 📝 Логирование

API использует **Loguru** для логирования:
- Логи в консоль с цветным форматированием
- Логи в файл `logs/api_debug.log` с ротацией (10 МБ)
- Автоматическое сжатие старых логов

## 🔐 Аутентификация

Проект использует JWT токены для аутентификации. Получите токен через эндпоинт `/users/login` и используйте его в заголовке:

```
Authorization: Bearer <your-jwt-token>
```

## 💾 Структура данных

Данные приложения хранятся в папке `launcher/DATA`:

```
DATA/
├── java/              # JDK версии
├── modpacks/          # Модпаки
├── libraries/         # Игровые библиотеки
├── assets/            # Ресурсы (текстуры, индексы)
├── players/           # Скины и аватары игроков
└── modpacks.json      # Метаданные модпаков
```

<!-- ## ✨ Бухфлип

![buhFlip-4x](https://github.com/user-attachments/assets/d286f39e-3d85-4a8c-b98f-c26d498f2e61) -->